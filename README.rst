django-model-extra-form
=======================

It makes django model flexible without changing table structure.

Installation
------------

.. code-block:: bash

    $ pip install django-model-extra-form
